/*

** Redux

* middleware: redux-promise, thunk, logger
* createStore(reducers, applyMiddleware([...middlewares]))
* bindActionCreators({...actions}, dispatch) // dispatch from react-redux
	every action creators wrapped into a dispatch call, then invoke directly
	
* combineReducers
* 

*/