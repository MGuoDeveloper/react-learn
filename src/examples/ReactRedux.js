/*

** React-Redux

* Provider (wrap your root component, pass store)
* connect(mapStateToProps, mapDispatchToProps)(Component)
	connect your component to store
* mapStateToProps
	encapsulating complex lookups or computations in selector.
	and using Reselect to write memoized selectors.
	
*/